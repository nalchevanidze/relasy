{
  "private": true,
  "workspaces": [
    "packages/*",
    "actions/*"
  ],
  "scripts": {
    "build:core": "npm -w @releasy/core run build",
    "build:action": "npm -w @releasy/draft-release-action run build",
    "build": "npm run build:core && npm run build:action"
  }
}
